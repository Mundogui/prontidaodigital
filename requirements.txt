# Framework principal
Flask==3.0.3

# Banco de dados SQLite (incluso no Python padrão, mas alguns pacotes o expandem)
# sqlite3 já vem integrado ao Python, portanto não é necessário instalá-lo separadamente

# Manipulação numérica
numpy==1.26.4

# Visualização de dados (matplotlib e plotly)
matplotlib==3.9.2
plotly==5.24.1

# Utilitários para encoding e buffer (já incluídos no Python padrão)
# base64 e io são módulos nativos — não precisam de instalação

# Caso use templates HTML (Jinja2 é dependência do Flask)
Jinja2==3.1.4
Werkzeug==3.1.3
itsdangerous==2.2.0
click==8.1.7
